imagePullPolicy: IfNotPresent

images:
  controlPlane: agentforge/control-plane:dev
  agentRuntime: agentforge/agent-runtime:dev
  webConsole: agentforge/web-console:dev
  sandboxExecutor: agentforge/sandbox-executor:dev

replicaCount: 1

service:
  webPort: 80

config:
  databaseUrl: jdbc:postgresql://host.k3d.internal:5432/agentforge
  databaseUser: agentforge
  kafkaBootstrapServers: host.k3d.internal:29093
  redisHost: host.k3d.internal
  redisPort: "6379"
  s3Endpoint: http://host.k3d.internal:9000
  runtimeCheckpointDsn: postgresql://agentforge:agentforge@host.k3d.internal:5432/agentforge
  clusterDns: 10.43.0.10
  # Nginx's runtime resolver does not apply the pod DNS search suffixes.
  controlPlaneHost: agentforge-control-plane.agentforge.svc.cluster.local:8080
  devAuth: true
  githubAppId: "0"
  githubAppSlug: ""
  githubApiVersion: "2026-03-10"

secret:
  existingSecret: ""
  databasePassword: agentforge
  redisPassword: agentforge
  executorToken: replace-for-any-shared-environment
  modelApiKey: ""
  githubClientId: ""
  githubClientSecret: ""
  githubPrivateKey: ""

resources:
  controlPlane:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: "1", memory: 1Gi }
  agentRuntime:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: "1", memory: 1Gi }
  webConsole:
    requests: { cpu: 25m, memory: 32Mi }
    limits: { cpu: 250m, memory: 128Mi }

ingress:
  enabled: true
  className: traefik
  host: agentforge.localhost
